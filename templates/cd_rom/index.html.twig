{% extends 'base.html.twig' %}

{% block title %}CDRom index
{% endblock %}

{% block body %}

	<div class="jumbotron">
		<div class="container">
			{{ form_start(form) }}
		</div>
		<div class="col">
			<div class="form-group">
				<button class="btn btn-primary">Rechercher</button>
				{{ form_end(form) }}
			</div>

		</div>
	</div>
</div>


<h1>CDRom index</h1>

<table class="table" id="lstindex">
	<thead>
		<tr>
			
			<th>Titre</th>
			<th>Cote</th>
			<th>Disponibilité</th>
			<th>Image</th>
			<th>Auteur</th>
			<th>Caution</th>
			<th>action</th>
		</tr>
	</thead>
	<tbody>
		{% for c_d_rom in c_d_roms %}
			<tr>
				
				<td><a href="{{ path('c_d_rom_show', {'id': c_d_rom.id}) }}">{{ c_d_rom.title }}</a></td>
				<td>{{ c_d_rom.cote }}</td>
				<td>{{ c_d_rom.availability ? 'Yes' : 'No' }}</td>
				<td>{{ c_d_rom.picture }}</td>
				<td>{{ c_d_rom.authtor }}</td>
				<td>{{ c_d_rom.bail }}</td>
				<td>
					
					<a href="{{ path('c_d_rom_edit', {'id': c_d_rom.id}) }}">edit</a>
				</td>
			</tr>
		{% else %}
			<tr>
				<td colspan="8">no records found</td>
			</tr>
		{% endfor %}
	</tbody>
</table>

<a href="{{ path('c_d_rom_new') }}">Create new</a>{% endblock %}{% block javascripts %}

       {{ knp_pagination_render(c_d_roms) }}
<script>
	{% include "filter/_filters.js" %}</script>
${% endblock %}
